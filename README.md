# Responsive Form

A mobile-first RTL contact form built with vanilla HTML, CSS, and JavaScript.

## Features

- Responsive design with CSS variables and flexbox
- RTL support (Hebrew)
- Client-side form validation
- Accessible (ARIA attributes)

## Run

```bash
npx serve src
```
